# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty of source code — it contains only a `README.md` and `LICENSE`. Per the README, the intended purpose is:

> An EAP-AKA protocol and TACACS+ server

There is no build system, package manifest, source tree, or test suite yet. When implementation begins, update this file with:
- The language/toolchain chosen and the actual build, lint, and test commands (including how to run a single test)
- The high-level architecture once one exists (e.g. how the EAP-AKA and TACACS+ components are organized, shared vs. protocol-specific code, entry points)
